# Production-readiness audit

**Status:** active; the project is not production-ready  
**Audit started:** 2026-08-11  
**Tracking epic:** [TokitoAI/tokito#336](https://github.com/TokitoAI/tokito/issues/336)

This is the evidence ledger for DS-ViRe and its Tokito integration. A green test
suite is evidence for the behavior those tests cover; it is not evidence that
the retrieval system, corpus, deployment, or user workflow is production-ready.

## Evidence baseline

| Evidence | Result | Scope / limitation |
|---|---|---|
| Release-wide verification | 233 passed, 1 skipped; strict mypy across 40 source files; Ruff format/lint clean; hosted run `31644992132` passed the release verifier, build/audit, hostile-PDF and robustness gates, production-boundary load, two no-cache container builds with rootfs comparison, image evidence, container robustness, license audit, and unsafe-config rejection | Unit/fixture/API/evaluator, immutable-artifact, cross-repository acceptance, and production-boundary tests; held-out accuracy is recorded separately below and live worker-enabled production acceptance remains credential-gated |
| Local Uvicorn smoke, one worker | readiness 200; unauthenticated evidence request 401; authenticated malformed PDF 422; process and scratch cleaned | Windows process boundary; not Linux container/resource-limit evidence |
| Frozen dependency graph | Universal `uv.lock` SHA-256 `673e2dd4...4588` covers runtime/test/visual/OpenCLIP and constrains one NumPy line across Python 3.11/3.12; runtime export SHA-256 `75add53d...0e4a` contains exact versions and artifact hashes; local release verifier passes 182 tests, strict typing across 25 package/script files, lint/format, no-isolation build, and hash-pinned runtime audit | Docker boundary/build evidence must come from Linux CI because Docker is unavailable on this workstation; byte-identical image output has not yet been demonstrated |
| v0.2.0 release CI | Green: lint/format, 108 tests, package build/audit, hash-pinned real-PDF identity gate, Docker fail-closed + authenticated worker smokes, provenance/SBOM | Boundary, packaging, and small development-gate evidence; not representative corpus accuracy, load, or end-to-end user workflow evidence |
| Production v0.2.0 rollout | Exact GHCR digest `sha256:2e847017cd7fe3cc554d7ef37050b5940ee6e63886e065dfa11cce82791a73d3` deployed privately; readiness 200, unauthenticated evidence 401, authenticated malformed PDF 422, zero restarts; official TPS5430 exact identity accepted with three regions and wrong package abstained; timestamped config/data backup and v0.1.1 rollback retained | Boundary and one real-document smoke; rollback image retained but restore/rollback drill not yet executed |
| Corpus and held-out visual policy | Hash-pinned visual registry of 279 cases across 40 official document families, 14 manufacturer labels, and 32 component categories: 30 development, five family-isolated calibration, and five reviewed evaluation families. The versioned source-free coverage policy records 40/500 documents, 90/2,000 natural-language queries, and 0 independent-human-reviewed documents. Query registry v2 binds every development prompt to graded relevant regions and explicit same-family hard negatives. A strict ranking contract and model-independent evaluator now compute nDCG@5, R@5, mAP, MRR, abstention/coverage, and hard-negative exposure; the checked perfect-score result is explicitly a closed-pool metric canary, not retrieval accuracy. The frozen text-layout evaluation accepted 7/15 positives (46.7%), zero wrong figures, and zero wrong identities, so `gate_passed=false`. | The 30-family minimum and leakage-safe split are met, but this is not independent human annotation or a representative corpus. The 90 queries are deterministic-template development records; none are manual, independent, calibration, or held-out data. No full-corpus system ranking has been measured with this contract. Automated publication remains disabled and the scored evaluation set is barred from tuning. No vendor bytes are checked in. |
| Exact-identity real-PDF gate (v0.2.0 release) | 3/3 exact identities emitted pinout, table, and package regions; 6/6 MPN/package negatives abstained with the expected reason; zero silent wrong-identity acceptances | All three groups are development data from one vendor, executed 2026-08-11; not a held-out accuracy or calibration estimate |
| v2 verifier-provenance real-PDF gate | 3/3 positives and 6/6 reason-matched negatives; zero silent wrong-identity acceptances; result schema `dsvire.identity-eval-result.v2` | Proves the breaking contract migration preserves the small development gate; it does not add visual verification or held-out calibration |
| v0.3.0 paired release/deployment | DS-ViRe release CI green (111 tests, lint/build/audit, Docker boundaries, 3-positive/6-negative real gate); exact DS-ViRe digest `sha256:3d4129179d8ba61761fc172a4f319ef4e556cd11581a2d6b657aaefc3511f6f6` deployed with tokito-ai v0.7.0 digest `sha256:084bae1ef433020e763fddb996f7f0be1b45350366ee8d60ff73045d80bae8f7`; both containers healthy with zero restarts; private health/readiness 200, unauthenticated extraction 401, authenticated malformed input 422, public API/MCP health 200; real TPS5430 v2 accepted by retrieval, wrong TSSOP package abstained, and the live publication endpoint rejected its heuristic-only evidence with 422 `evidence_rejected`; timestamped paired data/config backup and v0.2.0/v0.6.1 rollback images retained | Proves the versioned producer/consumer boundary and fail-closed publication policy in production; does not prove EGVV accuracy, restore success, rollback execution, load, or user upload orchestration |
| v0.3.1 repaired-PDF release/deployment | Release CI green (113 tests, lint/format, build/audit, 3-positive/6-negative real gate, Docker boundaries, provenance/SBOM); exact digest `sha256:cbcdff9bc60eda7608b28e020b359ddf6d16b4d01165381d9384d5be4c1a6626` deployed privately; service version/readiness 200, healthy with zero restarts; generated truncated-xref PDF rejected 422 with the stable repair error; official TPS5430 emitted v2 under `dsvire-baseline@0.3.1`, wrong TSSOP package abstained; public AI/MCP health 200; backup `/opt/tokito/backups/20260812T001600Z` validated, immediate v0.3.0 rollback retained, stale backups removed, zero dangling images, 35 GiB host space free | Proves this repair-path gate and deployment; broader malformed/scanned/rotated/parser-differential corpus, restore drill, and load evidence remain open |
| OpenCLIP visual-encoder candidate | Exact 605,143,316-byte LAION ViT-B-32 safetensors artifact pinned by commit/SHA-256. Two local Windows runs and clean GitHub runs `31538248265` / `31538257537` (attempt 2) all produced score digest `dae68875ad4952fdc7e96d35c893fb8ccb22a8239e61d8cbd5b2d5c95bccb0ff`; both GitHub artifact checksums validate; clean runtimes 5.05 s / 7.07 s, peak RSS 1.485 GB / 1.485 GB, external cost $0; 163 tests pass and the resolved extended environment has no known audited vulnerabilities. Run `31538257537` attempt 1 correctly failed closed after three TI CDN hash mismatches and emitted no result artifact. | Reproducible candidate comparison and source-integrity evidence, but not an acceptable identity verifier: wrong-variant mean similarity is close to positive and wrong-view mean is higher than positive; seed remains unreviewed single-vendor development data |
| First multi-vendor visual tranche | Five official, hash-pinned development families and 34 cases were contact-sheet inspected and owner-authorized agent-audited. After correcting the rejected ATmega crop, a frozen refresh produced text-layout digest `32bf8306...183bc`, 0.527 s, 9.49 docs/s, 69.4 MiB RSS; RapidOCR digest `d3938fad...0695f`, 116.12 s, 0.043 docs/s, 632.8 MiB RSS. RapidOCR positive/wrong-variant means are 0.796/0.484. Full compact export and chart are committed. | Useful adversarial and operability evidence only; this is not independent human annotation, no threshold was fitted, and wrong-package/wrong-figure separation remains unsafe |
| Public reproducible examples | `scripts/generate_docs_assets.py` deterministically derives a source-free evidence summary, evidence-bundle visual, and benchmark graph from the schema-tested fixture and frozen comparator result. CI byte-compares regenerated output, and `docs/EXAMPLES.md` names the exact commands, values, score semantics, and publication limitations. | Documentation evidence only; it neither redistributes vendor source pixels nor upgrades development results into held-out accuracy evidence |
| Durable Tokito orchestration release | `tokito-ai#60` adds bounded tenant/project/design uploads, durable SQLite jobs and blobs, fenced leases and heartbeats, retry/backoff, cancellation, restart recovery, exact-source verification, idempotent publication, terminal cleanup, and worker readiness. `tokito-ai#61` projects bounded per-design job state through the existing Companion journal/SSE. `tokito#371` adds desktop upload, cancellation, realtime status, and HTTP reconciliation. Cloud v0.9.2 and desktop v0.0.10 release workflows passed; v0.0.10 produced Windows, Linux, and universal macOS artifacts. | Cloud v0.9.2 is deployed fail-closed with the worker explicitly disabled because its separate extractor credential remains unprovisioned. No authenticated production upload-to-publication smoke or load evidence exists yet. |
| Seeded Wave D product acceptance | One Windows run crossed the real compiler, authenticated local Cloud ingestion, immutable generated store, generated-catalog packer, MCP streamable HTTP resolve/provenance, and Desktop place/save/reopen/hydration. All 29 contract findings passed; the eight-pin symbol retained package/MPN/manufacturer/datasheet metadata, two evidence region IDs, and byte-identical compiler text after reopen. The compact digest-bound report records the exact runtime and is [`examples/wave-d-acceptance.json`](../examples/wave-d-acceptance.json). | The checked EGVV evidence/spec pair is the explicit seed and no model call occurs. This closes deterministic integration wiring, not live extractor accuracy, production worker deployment, user upload, Companion convergence, or saturation evidence. |
| Cloud worker operational metrics and dashboard | `tokito-ai#64/#74` provide dedicated-bearer Prometheus metrics for durable queue/status/retry-wait/oldest-age state, process-local attempts/retries/outcomes/lease loss/stages, and explicit worker readiness/running/active/error state. Cloud v0.9.2 release run `31627997405` passed the full workspace, strict lint, production image, authenticated container smoke, GHCR publication, and release creation; exact digest `sha256:6891969fad01bbfbb3e59a725fa3276928c6024ed06e036030d3cea279627715` is deployed. Infrastructure main `b0fe49c` passed CI run `31629992636` and deploys loopback-only, resource-capped Prometheus v3.13.2 and Grafana v13.1.3 with persistent storage, a dedicated bearer-file scrape, six promtool-tested alert rules, and the ten-panel `tokito-dsvire-ops` dashboard. Production acceptance: target `up=1`, worker-ready `0` as expected while deliberately disabled, six rules loaded, zero firing alerts. | Metrics and local dashboard/rule evaluation are live. No operator-approved external notification receiver or pager delivery test exists; trace backend/exporter, redacted confidence distributions, provider cost accounting, and an incident-debug exercise remain absent. |
| Cloud recovery tooling and drill | `tokito-ai#65` (`4f1bfc6c0430d96c55c25b235e55ecd396a96331`) ships `tokito-ai-backup`: SQLite online snapshots, a versioned per-file SHA-256 manifest, safe regular-file extraction, non-overwriting staged restore, and database-to-blob digest checks for Companion and retained DS-ViRe sources. Linux/Windows CI, strict Clippy, cargo-deny, and adversarial tests passed. An isolated synthetic VPS drill built the exact PR commit into a disposable production image, then created, independently verified, and restored an archive in 546/458/480 ms; restored SQLite `quick_check` passed, its referenced source digest matched, and an in-flight fragment was excluded. Drill state/images were removed and production volumes were never mounted. | Proves executable Cloud job/artifact recovery, not recovery of live customer state. The runbook sets initial RPO 24h/RTO 2h and requires encrypted off-host retention, but scheduling/replication/monitoring are not deployed; DS-ViRe pack/config restoration, schema migration, production rollback, and full authenticated post-restore probes remain open. |
| Generated PDF robustness corpus | Manifest `dsvire.robustness-corpus.v1` binds 11 source recipes to stable production outcomes and strict-pypdf observations. Two local complete runs reproduced all source SHA-256 values; the release-wide verifier passed 187 tests plus the separate corpus gate and records manifest SHA-256 `a9a473fa92ff9f588ab9ba0bdaf5164a3733bcea1e4fa73dc6d0447557f4e64e` with case count 11. | Deterministic regression evidence only. It does not yet cover mutation/property fuzzing, hostile object streams/fonts/images, another renderer, or resource/timeout distributions. |
| Authenticated service-boundary load evidence | Versioned harness starts real Uvicorn and crosses bearer auth, TCP request parsing, one-slot admission, spawned workers, pack creation/cache validation, and cleanup using deterministic generated PDFs. Linux run `31632587220` at commit `ab36220a` passed the full production gate and published schema-valid artifact `9155724717` (SHA-256 `4e7a7cf...f799`): 12 requests, 7 successes, 5 bounded 503s, zero unexpected; cold/warm p95 623.588/612.045 ms, cached steady samples 202.818–219.750 ms, 174,796,800-byte peak process-tree RSS, four packs/261,650 bytes, zero scratch/partial residue. | Single four-vCPU hosted runner, generated two-page inputs, and 12 requests. This is not representative vendor-document capacity, soak, availability, or Tokito Cloud/extractor/catalog/Companion E2E evidence. The 800 ms Technical Bible SLO names the future hot-pack MaxSim query and is explicitly not applicable to this indexing boundary. |
| Retrieval implementation | PyMuPDF text blocks + deterministic heading/pin-token heuristics | No layout model, visual embedding, reranking, OCR scan path, or benchmark |
| End-to-end product pieces | The seeded Wave D acceptance now proves compiler → authenticated ingestion → generated store → packer → MCP resolve/provenance → Desktop exact save/reopen as one executable run. Durable Cloud upload/jobs, Companion realtime projection, and Desktop upload/status/cancel/reconciliation also exist. | Cloud v0.9.2 is deployed fail-closed with its DS-ViRe worker disabled because the separate extractor credential is not provisioned; live extraction, authenticated production upload through publication, Companion convergence, and independently reviewed UI/device acceptance remain unproven. |
| Container validation on this workstation | Not run: Docker CLI is unavailable | CI container evidence is required before merge |

## Severity rules

- **P0:** unsafe publication/corruption/auth behavior or a blocker to trustworthy use.
- **P1:** serious correctness, reliability, security, integration, or reproducibility gap.
- **P2:** important production capability or maintainability improvement.
- **P3:** optimization or polish after the system is trustworthy.

`Resolved` means a linked change and its stated verification exist. It does not
mean the broader workstream is complete.

## Findings

| ID | Severity | Status | Finding | Required evidence to resolve |
|---|---:|---|---|---|
| DSV-001 | P0 | Resolved in v0.1.1 | Hosted authentication was fail-open when `DSVIRE_SERVICE_TOKEN` was empty. | Startup-failure, unauthorized-request, and container tests with documented explicit local-only override. |
| DSV-002 | P0 | Resolved fail-closed: [#338](https://github.com/TokitoAI/tokito/issues/338), [#350](https://github.com/TokitoAI/tokito/issues/350) | Evidence v2 makes manufacturer, exact MPN, package association, package crop, verifier policy, and every accepted region explicit. Family/package/view adversaries are split by document family. The frozen policy's held-out run had zero silent wrong-identity acceptances; because coverage missed its floor, publication remains disabled rather than weakening identity requirements. | Reopen only if a producer/consumer bypasses evidence v2 or publication is enabled without a newly pre-registered policy passing its held-out gate. |
| DSV-003 | P0 | Fail-closed evidence: [#338](https://github.com/TokitoAI/tokito/issues/338), [#350](https://github.com/TokitoAI/tokito/issues/350), [#356](https://github.com/TokitoAI/tokito/issues/356) | The split-safe benchmark compared text-layout, RapidOCR, and hash-pinned OpenCLIP on calibration, then froze text-layout before evaluation. Held-out evaluation accepted 7/15 positives (46.7% coverage), zero wrong figures, and zero wrong identities. This misses the frozen 50% coverage minimum, so `gate_passed=false`; the threshold was not retuned and automated publication remains disabled. | A future version needs a newly pre-registered benchmark/model cycle; this evaluation set must not be reused for tuning. |
| DSV-004 | P1 | Resolved in v0.1.1 | Untrusted PDF parsing ran inside the API process with no job admission bound or killable timeout. | Subprocess isolation, kernel limits, hard timeout/cancellation, bounded admission, cleanup, overload and crash tests. |
| DSV-005 | P1 | Resolved in v0.1.1 | Concurrent requests wrote directly into the same pack directory and failures could leave partial artifacts. | Identity/version keyed cache, lock, staging directory, atomic publication, integrity recheck, concurrent regression test. |
| DSV-006 | P1 | Partial: [#342](https://github.com/TokitoAI/tokito/issues/342), [#350](https://github.com/TokitoAI/tokito/issues/350), [#357](https://github.com/TokitoAI/tokito/issues/357), [#358](https://github.com/TokitoAI/tokito/issues/358), [tokito-dsvire#74](https://github.com/TokitoAI/tokito-dsvire/issues/74), [tokito-dsvire#76](https://github.com/TokitoAI/tokito-dsvire/issues/76), and [tokito-dsvire#78](https://github.com/TokitoAI/tokito-dsvire/issues/78) | Strict identity, visual, and query registries enforce group-level split leakage checks, download-only policy, hash verification, and deterministic metric semantics. Query registry v2 binds graded relevance and query-specific hard negatives, including same-part wrong-intent figures. A digest-bound ranking contract rejects missing, duplicate, injected, cross-query, and unsorted candidates. The model-independent evaluator reports nDCG@5, R@5, mAP, MRR, coverage/abstention, and hard-negative exposure by split and intent. Its checked relevance-first result is explicitly a closed judged-pool contract canary, not retrieval accuracy. The ledger remains 40/500 documents and 90/2,000 deterministic-template development queries with 279 cases, 14 manufacturers, 32 categories, and 0 independent-human-reviewed documents. | Run actual full-corpus retrievers through the contract; expand representative/legal coverage without reusing scored evaluation families for tuning; add diverse manual development queries, obtain independent held-out review, and add scheduled immutable release-policy evidence. |
| DSV-007 | P1 | Partial in v0.3.1 plus [#386](https://github.com/TokitoAI/tokito/issues/386) and [#392](https://github.com/TokitoAI/tokito/issues/392) | Parser-repaired documents fail closed before publication. The versioned 11-case generated corpus covers stable semantic outcomes and strict-pypdf differential observations. A second deterministic 48-case campaign mutates content bits/runs, truncation, duplicated slices, xref/stream/object/font/image tokens, and appended bytes through the real spawned-worker boundary. It enforces 5 s wall time, 3 s CPU, 512 MiB address space, 64 MiB output, bounded diagnostics, scratch cleanup, and atomic publication invariants. Linux CI run `31572372989` reproduced digest `6a17b0bd...e74b`: 16 safe accepts/32 rejections, zero worker errors/timeouts, max 726 ms, p95 708 ms, and 122,474,496-byte peak child RSS; the exact JSON is retained as artifact `9131860841`. Local Windows reproduced the same campaign/outcomes with max/p95 887/877 ms and 94,134,272-byte peak child RSS. | Add coverage-guided/property fuzzing over larger and structurally targeted object-stream/font/image corpora, another renderer/parser differential, sanitizer builds for native dependencies, and longer soak evidence. |
| DSV-008 | P1 | Partial via [#361](https://github.com/TokitoAI/tokito/issues/361) | Cloud v0.9.2 is deployed and exposes bounded authenticated upload, list/get/cancel APIs plus ordered Companion journal/SSE state. Desktop v0.0.10 supplies upload, cancel, realtime status, and HTTP reconciliation after reconnect/restart. Tenant/project/design authorization and partial-failure paths are covered below the live boundary. Production deliberately runs with the worker disabled because its required dedicated extractor credential is not provisioned; authenticated uploads fail before staging bytes with stable 503 `dsvire_worker_unavailable`. | Provision the dedicated extractor credential and enable the worker, then pass an authenticated production upload-to-publication smoke and independently verify Desktop/Companion convergence across reconnect, restart, cancellation, and idempotent replay. |
| DSV-009 | P1 | Partial via [#361](https://github.com/TokitoAI/tokito/issues/361), [#390](https://github.com/TokitoAI/tokito/issues/390), and [#413](https://github.com/TokitoAI/tokito/issues/413) | Cloud's durable-store contention gate remains 256 exact admits/256 rejects plus idempotent replay at ~219 jobs/s. The separate DS-ViRe Linux service gate now crosses authenticated HTTP, one-slot admission, spawned PDF workers, persistent pack creation/cache reads, overload, and cleanup: 7 successes, 5 bounded 503s, zero unexpected/residue, cold/warm p95 623.588/612.045 ms, and 174,796,800-byte peak process-tree RSS. | Production deployment/restart drill and representative end-to-end saturation across Cloud upload, blob I/O, DS-ViRe, extractor, publication, and Companion remain required. Neither the store microbenchmark nor 12 generated service requests is capacity/availability/soak evidence. |
| DSV-010 | P1 | Partial via [#380](https://github.com/TokitoAI/tokito/issues/380), [#391](https://github.com/TokitoAI/tokito/issues/391), and [#410](https://github.com/TokitoAI/tokito/issues/410) | Cloud v0.9.2 exposes bounded queue/status/retry-wait/oldest-age and worker-readiness gauges plus attempt/retry/outcome/lease-loss/stage counters behind a dedicated metrics bearer. Strict W3C context remains durable and propagates to Companion and private services. Production Prometheus/Grafana now scrape the target successfully and provide a ten-panel dashboard plus six fixture-tested alerts; the safe disabled-worker/empty-queue state produces no alert. All services are loopback-only and resource-capped. | Configure and test an operator-approved external alert receiver; add a trace backend/exporter, redacted confidence distributions, provider cost accounting, and a documented incident-debug exercise. |
| DSV-011 | P1 | Partial via [#359](https://github.com/TokitoAI/tokito/issues/359), [#387](https://github.com/TokitoAI/tokito/issues/387), [#388](https://github.com/TokitoAI/tokito/issues/388), and [#389](https://github.com/TokitoAI/tokito/issues/389) | The universal/hash locks, digest-pinned base, frozen workflows, provenance/SBOM, and no-resolution image remain enforced. The exact 24-package license policy fails drift and blocks tagged releases on unresolved decisions. On exact commit `64778a96`, GitHub-hosted run `31567938838` and isolated-runner run `31567940882` produced four distinct image IDs but the same 7,754-entry normalized rootfs SHA-256 `886bd752dd54fd0a8d0d5ceb15d0e780587d5be39a75296feb2fefac7581a534`, with zero within- or cross-builder path/metadata/content differences and successful cleanup. | Resolve PyMuPDF 1.28.2's declared AGPL-3.0-or-later/commercial disposition with documented commercial coverage or counsel-approved AGPL compliance. |
| DSV-012 | P1 | Partial via [#384](https://github.com/TokitoAI/tokito/issues/384) | Cloud v0.8.x now contains verified backup/restore tooling and a documented offline recovery process with initial RPO 24h/RTO 2h, retention, encryption-boundary, post-restore probes, observation, and rollback requirements. An isolated synthetic production-image drill passed create/verify/restore plus SQLite and blob-integrity checks. | Deploy automated encrypted off-host backup scheduling, replication/failure monitoring, and retention; recover copied production-like state through authenticated API/Companion probes; drill DS-ViRe pack/config restoration, schema migration, and actual deployment rollback. |
| DSV-013 | P1 | Resolved in DS-ViRe v0.3.0 / tokito-ai v0.7.0 | Evidence v2 requires accepted pinout, table, and package regions plus identity grounding provenance. The producer, canonical Rust contract, extractor context policy, publication gate, cross-repository tests, paired immutable releases, and live fail-closed deployment are complete. Desktop and MCP consume published catalog records rather than parsing evidence bundles, so they required no wire migration. | Versioned contract decision implemented by every producer/consumer with cross-repository contract tests. |
| DSV-014 | P2 | Resolved by [#360](https://github.com/TokitoAI/tokito/issues/360) | Mypy now runs in strict mode across every package module and operational script, with typed adapters at untyped native/ML boundaries. One version-pinned release command fails fast across dependency/export drift, typing, lint, formatting, bytecode compilation, all tests and committed schema/registry/benchmark contracts, isolated package construction, and the hash-pinned runtime vulnerability audit. CI and tagged releases publish its bounded JSON stage/timing report before container checks. | Pinned type/build gates and one release-wide verifier with failure-propagation tests and published CI evidence. |
| DSV-015 | P2 | Open | The baseline API is synchronous per request and persists query artifacts locally; lifecycle, quotas, tenant isolation, and object-store ownership are not defined. | Ownership ADR, tenant/authz tests, quotas, lifecycle policy, and production storage integration. |

## Production gate matrix

| Area | Current verdict | Completion evidence |
|---|---|---|
| Accuracy and calibration | Fail-closed / insufficient | A leakage-safe held-out run exists and proves zero wrong-figure/wrong-identity accepts, but 46.7% positive coverage misses the frozen 50% floor. Automated publication correctly remains disabled; a new candidate requires a newly pre-registered cycle. |
| Determinism | Partial | Byte-identical compiler fixture exists; retrieval/cache reproducibility needs corpus evidence |
| Security | Partial | Service boundary hardening underway; parser, model/tool, dependency, tenant, and deployment threat model remains |
| Resilience and recovery | Partial | Durable jobs, restart/resume, retry, cancellation, graceful shutdown, one successful failed-canary rollback, and a verified synthetic Cloud job/blob restore drill exist; automated off-host backups and production-like job/pack recovery plus rollback drills remain |
| Observability | Partial | Cloud v0.9.2 metrics and durable trace context are deployed; Prometheus scrapes successfully and Grafana provisions a ten-panel dashboard with six tested local alert rules. External notification delivery, a trace backend/exporter, cost/confidence telemetry, and an incident exercise remain. |
| Scalability and efficiency | Partial | Cloud store contention and a real DS-ViRe HTTP/worker generated-input load gate now record latency, throughput, bounded overload, RSS, output bytes, and cleanup. Representative vendor-document/E2E saturation, soak/availability, GPU/index throughput, pack-size Pareto, and the future hot-pack MaxSim SLO remain open. |
| Maintainability | Partial | Frozen schemas/tests exist; contract drift, locking, typing, and release gate remain |
| Reproducibility | Partial | Dependency/model/source locks and deterministic benchmark identities exist; corpus completion and independent byte-identical image proof remain |
| Deployment | Partial | Immutable Cloud v0.9.2 is healthy in production with a digest-pinned image and resource-capped loopback monitoring. Its DS-ViRe worker remains deliberately disabled pending the dedicated extractor credential; worker-enabled E2E, recovery, and rollback acceptance remain. |
| Tokito workflow | Partial | Seeded Wave D now proves exact generated-symbol publication/MCP/Desktop roundtrip in one run. Desktop v0.0.10 and Cloud v0.9.2 also implement upload/jobs/Companion state; Cloud is deployed with the worker disabled pending its dedicated extractor credential, so live authenticated user E2E and device convergence remain absent. |

## Decision log

1. **2026-08-11 — keep PDF parsing outside the API process.** PDF bytes are
   untrusted, parsing is CPU-heavy native code, and thread timeouts cannot stop a
   wedged parser. A disposable spawned process supplies a kill boundary while
   Linux resource limits constrain common exhaustion paths.
2. **2026-08-11 — authentication fails closed.** Private-network placement is
   defense in depth, not authentication. Local insecure mode requires two
   explicit settings and is never valid in production/staging.
3. **2026-08-11 — do not call the heuristic verifier EGVV.** It is a useful
   deterministic baseline but cannot prove visual or exact-variant correctness.
4. **2026-08-11 — caller identity is a hypothesis, not evidence.** The hosted
   baseline must abstain unless bounded PDF text independently contains the
   manufacturer and associates the exact token-bounded MPN with the requested
   package in the same logical orderable-part row. This reduces silent near-miss publication;
   it does not replace OCR,
   visual verification, or held-out calibration.

## Next burn-down order

1. Keep automated catalog publication disabled until a newly pre-registered
   verifier cycle passes its held-out policy; never tune on the scored v1
   evaluation families (DSV-002/003).
2. Expand the legal corpus, independent review, and hostile-input evidence
   without contaminating the frozen evaluation set (DSV-006/007).
3. Deploy the released durable Tokito Cloud orchestration after provisioning the
   dedicated extractor credential, then execute production Desktop/Companion E2E
   and recovery acceptance (DSV-008/009).
4. Complete external alert delivery and trace/cost/confidence observability;
   add load/SLO evidence, resolve the license policy, and complete recovery drills.
